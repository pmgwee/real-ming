# Compose the existing agent systems

The system will use Hermes as its conversational and tool runtime, preserve Agent Brain as Project Evidence, and introduce Real-Ming as the policy, coordination, approval, and audit boundary. This composition was chosen over replacing the existing systems or adopting a second competing agent core because the current systems already contain working integrations and substantial history.
